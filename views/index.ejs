<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1 class="title"><%= pageTitle %></h1>
    <form action="/new">
      <button class="add-message" type="submit"> Add A New Message </button>
    </form>
    

    <div>
      <ul class = "message-list">
        
          <% for(let i=0; i<messages.length;i++){ %>
          <div class="message">
            <li class = "message-text"> <%= messages[i].name %> says:<br>
            <%= messages[i].text %><br>
            <%= messages[i].date %></li>
          </div>
          <% }%>
       
      </ul>
    </div>
  </body>
</html>
